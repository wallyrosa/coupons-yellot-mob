import { colors } from "./colors";

export const themeConfig = {
  extend: {
    colors,
    fontSize: { xxs: 10 },
    borderRadius: {
      lg: 16,
      md: 12,
      sm: 8,
      xs: 4,
    }
  },
};